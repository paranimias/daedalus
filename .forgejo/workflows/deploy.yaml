name: Deploy Blog

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: native
    steps:
      - uses: actions/checkout@v4

      - name: Deploy
        run: |
          /run/current-system/sw/bin/rsync -a --delete \
            ${{ gitea.workspace }}/_site/ /var/www/blog/
